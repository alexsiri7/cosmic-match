import 'package:flutter/material.dart';

// Lyra galaxy design tokens — sourced from src/theme.jsx GALAXIES.lyra
const Color kCosmicInk     = Color(0xFF0D0A1A);         // deep space bg
const Color kCosmicNebulaA = Color(0xFF4B2870);          // violet overlay ≈ oklch(0.38 0.18 300)
const Color kCosmicNebulaB = Color(0xFF244E7A);          // cyan-blue overlay ≈ oklch(0.42 0.15 220)
const Color kCosmicAccent  = Color(0xFFC87FE0);          // lavender accent ≈ oklch(0.82 0.15 300)
const Color kBoardBackdrop = Color(0x59000000);          // 35% black board panel
const Color kGridLine      = Color(0x0AFFFFFF);          // 4% white grid dividers (reserved — not yet applied)
